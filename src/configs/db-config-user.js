//es la configuracion de la tabla 

const config_user = { 
    host        : "localhost",
    database    : "dai-events-full",
    user        : "postgres", 
    password    : "root", 
    port        : 5432 
} 
export default config_user;